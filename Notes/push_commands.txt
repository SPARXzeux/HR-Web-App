cd "E:\Delcargo HR"
git add .
git commit -m "Fix TclError crash on tracker app relaunch: pady tuple passed to Label constructor instead of .pack()"
git push origin main

git tag tracker-agent-v5
git push origin tracker-agent-v5
# Release will appear at:
# https://github.com/SPARXzeux/HR-Web-App/releases/tag/tracker-agent-v5
# (tracker-agent-v4 was already used in the previous round, so this fix
# needed a new tag number — v4 itself was never updated with the crash fix)
